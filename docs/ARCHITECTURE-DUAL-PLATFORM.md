# WhimprFlow — Dual-Platform Architecture (Windows + macOS)

> Synthesized 2026-07-17 from a 12-agent all-Opus workflow (7 recon tracks, critic, 3 gap-fills, synthesis): dual-platform stack evaluation, a teardown of Wispr Flow's actual Windows build, Windows hotkey/insertion/shell engineering, and cross-platform ASR + LLM benchmarks by hardware tier. Raw tracks in docs/research/win-*.md and docs/research/xplat-*.md.
>
> **This supersedes the "fully native Swift" decision in SPEC.md.** SPEC.md's UI spec, Fn state machine, cleanup rule table, prompts, and feature matrix are unchanged in intent — only the implementation substrate moves to Tauri v2 + a shared Rust core. Read this alongside SPEC.md; the "Deltas" section below lists exactly what changes.


---

## Stack verdict

DECISION: Tauri v2 (Rust shared core + system webview), following Handy's architecture, with TWO deliberate research-forced forks off the pure-one-codebase ideal. || SCORED COMPARISON (weighted /125, from stack-decision track): Tauri v2 ~108 | Rust core + 2 native shells ~95 | Electron ~89 | Flutter ~63. Tauri wins the two decisive highest-weight axes: (a) ONE codebase / dev-velocity / parity (w3) = 5 because Handy is a feature-identical dual-platform reference (MIT), and (b) in-process ONNX ASR + llama.cpp LLM (w3) = 5 because transcribe-rs + llama-cpp-2 run in-process, GPU-accelerated, on both OSes; plus it is the only production-proven transparent, non-activating, click-through, above-fullscreen pill on BOTH macOS and Windows from one codebase (Handy overlay.rs). Electron is the safe heavy middle and Wispr's own choice, but ~800MB RAM (measured in Wispr's Windows build) vs Tauri ~40-120MB idle. Option-4 two-native-shells wins raw fidelity but loses ~13 points to the 2x-UI-codebase penalty against the ideally-ONE-codebase mandate. Flutter disqualified (no in-process ML, no mature Fn/CGEventTap/WH_KEYBOARD_LL-suppression plugin). || PRIOR FULLY-NATIVE-SWIFT DECISION IS KILLED. SPEC.md ARCHITECTURE 'CONFLICT RESOLVED - stack' chose fully-native Swift (AppKit NSPanel/NSStatusItem + SwiftUI NSHostingView). That has no Windows story; keeping it forces a second full native shell in C#/.NET (Wispr's split, which one-codebase forbids). Replaced by Tauri v2 + Rust core + React/webview UI: the same shared-UI-plus-thin-native-shim pattern Wispr uses (shared renderer + per-OS helper), but a smaller shell and a SHARED Rust helper instead of two hand-written ones. || FORK 1 (ASR) reverses the stack track's 'swap FluidAudio to transcribe-rs'. The gap-fill ASR track is decisive: single-engine ort/Parakeet on the CPU EP does NOT reliably meet the perceived ~300-500ms budget on mid-tier Apple Silicon, and the CoreML EP CANNOT run a Parakeet-TDT int8 graph on the ANE (the LSTM prediction net + int8 QDQ ops are unsupported and fall to CPU; the ~125-250-call Rust-driven greedy decoder loop would be a net regression). Measured: FluidAudio ANE = 0.1935s / ~145.8x RTFx (128.6x median, ~190x README) on M4 Pro; best-case ort-CPU Parakeet = ~30x on M4 Max (~400ms e2e) and only ~10-18x on a base M-series Air/M1 (~700-1000ms+ e2e, misses perceived and risks committed <900ms). Handy itself ships Parakeet ort CPU-only on every OS (Cargo.toml has features=['onnx'] only, never ort-coreml; Metal is added ONLY to transcribe-cpp/whisper.cpp). RESOLUTION: KEEP FluidAudio as a Mac-only ASR backend behind a shared AsrEngine trait from v1 (ANE latency+power), with transcribe-rs/ort CPU Parakeet as the Mac portable fallback AND the Windows primary. This forks 'one codebase' only at the ASR-engine seam - the minimal, natural place (Handy already models ASR as a swappable LoadedEngine enum). || FORK 2 (native hooks) chooses Wispr's sidecar over Handy's in-process model for the fidelity-critical native subsystems. The gap-fill native-hook track is decisive: co-locating the WH_KEYBOARD_LL / CGEventTap hook thread in the same process as saturating ONNX+llama.cpp inference creates two risks a dedicated in-process thread does NOT remove: (1) scheduler starvation past the 1000ms LowLevelHooksTimeout / CGEvent tap-timeout window under full-core inference, and (2) an inference crash/OOM kills the hook with the whole process. On Windows the resulting LL-hook removal is SILENT and UNDETECTABLE by design (Microsoft: 'no way for the application to know whether the hook is removed') and unrecoverable; handy-keys only re-hooks on suspend/session events, not on an inference stall. RESOLUTION: run the hook + text-injection + secure-input + AX/UIA context read in a SEPARATE native sidecar process, implemented as ONE shared Rust binary reusing handy-keys 0.3.0 + enigo 0.6.1 on both OSes (isolation with one language, closer to one-codebase than Wispr's Swift+C# split). Windows sidecar = hard requirement; macOS sidecar = strongly advised (in-process is survivable there because the CGEvent tap-timeout is recoverable/self-announcing, so an in-process handy-keys tap with the SPEC.md health watchdog is an acceptable macOS-only fallback if scope must shrink). Shell heartbeats the sidecar and respawns it on hang. || WHAT HAPPENS TO EACH REUSABLE MAC RECIPE: -- NSPanel flags (SPEC.md A): SURVIVE via tauri-nspanel PanelBuilder (ahkohd fork, branch v2.1). PanelLevel::Status = raw NSWindow level 25 (above menu bar); StyleMask borderless().nonactivating_panel(); collectionBehavior can_join_all_spaces().full_screen_auxiliary().ignores_cycle(); no_activate(true), focusable(false), transparent(true), shadow off, decorations(false). Byte-for-byte the OpenSuperWhisper IndicatorWindowManager recipe, reached through a plugin instead of Swift. Windows twin = WebviewWindowBuilder focusable(false) => WS_EX_NOACTIVATE + always_on_top => WS_EX_TOPMOST + force_overlay_topmost() SetWindowPos(HWND_TOPMOST, SWP_NOMOVE|SWP_NOSIZE|SWP_NOACTIVATE|SWP_SHOWWINDOW). -- CGEventTap engine (SPEC.md MACOS IMPLEMENTATION): SURVIVES but MOVES into the shared Rust sidecar. handy-keys uses a direct CGEventTap via objc2_core_graphics (NOT rdev) on a dedicated CFRunLoop thread, consuming (HotkeyManager::new_with_blocking), with a 100ms tap_is_enabled/tap_enable health poll and Fn keycode 0x3F flagsChanged handling. To reach full SPEC fidelity (bare-Fn suppression that kills the OS Globe action, 5s health timer with full remove->recreate->re-add reinstall, and a dedicated stale-held-key watchdog a la Wispr CheckStaleKeys) extend handy-keys or hand-roll a core-graphics/objc2 tap. -- FluidAudio (SPEC.md ASR ENGINE): RETAINED as the Mac primary ASR backend behind AsrEngine (see FORK 1); Apache-2.0 SDK, ANE-only, macOS 14+/iOS 17+, bundles Silero VAD v6.2.1. Called from Rust via a small Swift FFI shim or Swift sidecar; ort CPU Parakeet is the fallback if the CoreML model compile fails. -- llama.cpp embed (SPEC.md CLEANUP LLM RUNTIME): SURVIVES, now as llama-cpp-2 0.1.151 (utilityai/llama-cpp-rs, MIT/Apache-2.0) in the Rust core instead of linking libllama from Swift; features metal (macOS) + vulkan (Windows) both confirmed; loads GGUF, --mlock resident, in-process, no daemon. The optional MLX 'faster decode' toggle from SPEC.md is DROPPED for v1 (Swift-only, marginal for ~67-token outputs). Claude Haiku toggle = a stack-neutral reqwest HTTPS call (x-api-key + anthropic-version: 2023-06-01), exactly Handy's llm_client.rs pattern. || ELECTRON FALLBACK TRIGGER (documented, must be settled on real Win11 hardware): flip to Electron ONLY if the semi-transparent pill renders a black/opaque box AT REST (no resize, no focus change) in the shadow:false + decorations:false + creation-time-transparent + unparented config on any common GPU (Intel iGPU / NVIDIA / AMD) - i.e. Tauri issue #14823-class black edges reproduce on a static 256x46 window. Nothing in the issue corpus shows this without resize/shadow/decorations, but it is unverified for a static pill. Secondary trigger: product requires Wispr's permanently-large transparent hover container AND the grow-on-hover alternative is rejected (then Electron setIgnoreMouseEvents(true,{forward:true}) is marginally cleaner). Neither has been observed to fire.

---

## Shared core design

PRINCIPLE: maximize the shared Rust core; fork only at three seams the research forces (ASR-engine backend, native-hook/injection sidecar, and per-OS overlay pixel geometry). Handy already forks ~6 platform modules, proving 'one codebase' != 'zero per-OS code'. || SHARED CORE (identical Rust on both OSes, in the Tauri main process): -- State machine: the full IDLE -> RECORDING (push-to-talk) -> FINALIZE -> PASTE -> IDLE plus DOUBLE-TAP -> LOCKED (hands-free) plus Esc -> CANCEL logic from SPEC.md 'FN STATE MACHINE'. Platform-agnostic; fed by trigger tokens from the sidecar. Includes the 20-min session cap, 19-min warning, anti-bounce 500ms cooldown, and single-tap no-op gate. -- Audio pipeline: cpal capture (WASAPI on Windows / CoreAudio on macOS) -> downmix mono -> rubato 48k->16k resample -> vad-rs Silero VAD v6.2.1 (512-sample/32ms frames, threshold 0.5, speech_pad 150ms) -> 16kHz mono f32 buffer. Plus a device-hotplug watcher (cpal does NOT emit these): Windows IMMNotificationClient OnDefaultDeviceChanged / macOS CoreAudio kAudioHardwarePropertyDefaultInputDevice listener, else a re-enumerate poll. -- ASR orchestration: the AsrEngine trait + batch-finalize-on-release logic + long-hands-free rolling 20-30s chunk concatenation. (Backends themselves are per-platform - see seam 1.) -- Cleanup LLM: llama-cpp-2 0.1.151 in-process (Qwen3-4B-Instruct-2507 Q4_K_M, --mlock resident, prefill-on-hotkey-down, streaming) + the CleanupProvider trait with ClaudeCleanupProvider (reqwest, claude-haiku-4-5, thinking OFF, x-api-key). Both providers share the identical system prompt. -- Prompts: the entire WHIMPRFLOW CLEANUP PROMPTS section (system prompt, Level modifiers None/Light/Medium/High, few-shot, verifier, command/rewrite prompt) is data, 100% shared. -- Dictionary: manual vocab + 1 replacement rule/word; Double-Metaphone pre-filter injecting <=15 entries; auto-learn diff engine + wordfreq Zipf>=3.0 gate. Shared logic; only the 'read text around caret / focused element' feed is per-OS (AX on macOS, UIA on Windows - see seam 2). -- Cleanup gates + verifier + raw-ASR fallback: shared. -- Storage: SQLite (transcripts/dictionary/snippets, optionally encrypted) + tauri-plugin-store (settings JSON, replacing UserDefaults). Shared. -- UI: ONE React/webview bundle for all Flow Bar states (idle/listening/recording/locked/transcribing/formatting/processing/done/error), the Hub, Settings, Onboarding. 60fps 5-7-bar waveform computed with rustfft 6.4.0 in Rust, streamed to an HTML canvas throttled to ~30fps (Handy EMIT_THROTTLE_MS=33). This mirrors Wispr's own 'one shared JS bundle with process.platform branches' (113 process.platform refs, 84 win32, 59 darwin in Wispr's main). || SEAM 1 - ASR ENGINE (per-platform backend behind a shared trait): trait AsrEngine { fn transcribe(&self, pcm16k: &[f32]) -> Result<String>; fn warmup(); }. macOS impl = FluidAudioEngine (Swift/CoreML/ANE FFI shim, Parakeet TDT 0.6B v2) with an OrtParakeetEngine CPU fallback. Windows impl = OrtParakeetEngine (transcribe-rs 0.3.8 features=['onnx'], ort =2.0.0-rc.12, Parakeet V2 473MB int8 CPU default; DirectML/CUDA opt-in) with a transcribe-cpp whisper.cpp tiny/base fallback for the AVX2-floor tier. || SEAM 2 - NATIVE SIDECAR (one shared Rust binary, separate process): owns the global hotkey hook (handy-keys 0.3.0 + rdev rustdesk fork), text injection (enigo 0.6.1 + hand-rolled clipboard save/restore), secure-input/elevation detection, and the AX (macOS AXUIElement) / UIA (Windows IUIAutomation) context read + caret-rect read. Length-prefixed JSON over stdio (Wispr's proven contract; codegen the schema like Wispr's quicktype). Handlers mirror Wispr: DictationStart/Stop, PasteText, CheckStaleKeys, UpdateShortcuts, SecureInputStatus, plus Windows-only WindowsKeyUpSimulation and ShiftInsert. macOS-only fallback: the sidecar MAY collapse into the main process (in-process handy-keys tap) if v1 scope demands - Windows may not. || SEAM 3 - OVERLAY GEOMETRY (per-OS pixel tuning, shared React content): platform offset constants differ (Handy: macOS OVERLAY_TOP_OFFSET=46/BOTTOM=15; Windows TOP=4/BOTTOM=40), plus Windows fractional DPI (125/150/175%) and work-area/taskbar math. || CRATE / MODULE LAYOUT: workspace root -> crate whimpr-core (state machine, audio pipeline, ASR orchestration trait, cleanup LLM + providers, prompts, dictionary, gates/verifier, storage) | crate whimpr-asr-ort (transcribe-rs/ort Parakeet, both OSes) | crate whimpr-asr-fluidaudio (cfg(macos) Swift FFI shim) | crate whimpr-llm (llama-cpp-2 wrapper + reqwest Claude client) | crate whimpr-sidecar (bin: handy-keys + enigo + objc2/core-graphics [macos] + windows 0.61.3 [win] + stdio IPC) | crate whimpr-overlay (tauri-nspanel [macos] vs WebviewWindowBuilder+SetWindowPos [win], 3 cfg branches like Handy) | crate whimpr-tauri (Tauri v2 shell, tray, autostart, updater, capabilities) | ui/ (React/TS: overlay, hub, settings, onboarding). Tauri plugins: tauri-plugin-global-shortcut 2.3.1 (fallback only), tauri-plugin-clipboard-manager 2.3.2, tauri-plugin-updater 2.10.0, tauri-plugin-autostart, tauri-plugin-store, tauri-plugin-opener. cfg-gated deps: [target macos] tao patch pin if overlay needs it, objc2, core-graphics; [target windows] windows 0.61.3 (Win32_Media_Audio, Win32_UI_WindowsAndMessaging, Win32_UI_Accessibility), winreg 0.55.

---

## Windows spec (mirrors the Mac SPEC)

Complete Windows-side spec mirroring the Mac SPEC.md. Target Win10 1809+ / Win11 x64 (and note Win-on-ARM caveats). || HOTKEY ENGINE + DEFAULT COMBO: Default push-to-talk = Ctrl+Win (L/R agnostic), matching Wispr (docs.wisprflow.ai). Hands-free toggle = Ctrl+Win+Space (or double-tap Ctrl+Win). Command Mode = Ctrl+Win+Alt. Cancel = Esc while recording. Do NOT default to Fn on Windows (firmware-invisible on most PCs; a WH_KEYBOARD_LL hook, RawInput, RegisterHotKey, and rdev all fail to see bare Fn except on some ThinkPads via BIOS/WMI) - this is the key Mac(Fn)/Windows(Ctrl+Win) divergence. Engine = a hand-rolled WH_KEYBOARD_LL (idHook=13) hook via SetWindowsHookExW in the shared Rust sidecar (windows 0.61.3 crate), NOT RegisterHotKey (which reports PRESS only, needs a real vk, and cannot express a modifier-only combo) and NOT tauri-plugin-global-shortcut/global-hotkey v0.8.0 (RegisterHotKey-based, 50ms polled synthetic release, no modifier-only, no suppression). Callback on a dedicated hook thread with its own GetMessageW loop; on HC_ACTION read vkCode/flags from KBDLLHOOKSTRUCT, match the tiny trigger set (VK_LWIN 0x5B, VK_RWIN 0x5C, VK_LCONTROL 0xA2, VK_RCONTROL 0xA3, etc.), update an atomic state machine, push a token to a lock-free channel, then IMMEDIATELY return 1 (suppress owned keys) or CallNextHookEx - target single-digit-microsecond callback latency. Handle WM_KEYDOWN 0x0100 / WM_KEYUP 0x0101 / WM_SYSKEYDOWN 0x0104 / WM_SYSKEYUP 0x0105. Start-menu suppression: because Ctrl already dirties the Win press the Start menu usually will NOT open; robustly, once the trigger fires swallow the subsequent Win key-UP (WindowsKeyUpSimulation, exactly Wispr's IPC message). Tag self-injected events with a magic dwExtraInfo and ignore LLKHF_INJECTED (0x10) / LLKHF_LOWER_IL_INJECTED (0x02) to prevent feedback loops. Ship first-class alternatives: Ctrl+Alt, Right Ctrl, a spare key (Pause/ScrollLock/Insert/PageUp), and mouse Mouse4/Mouse5/middle. Hotkey validity rules identical to Mac SPEC (max 3 keys, >=1 modifier or valid mouse button, no L/R mix, no CapsLock, Esc standalone, ~40 reserved combos blocked). Watch session lock/unlock (WTSRegisterSessionNotification, WM_WTSSESSION_CHANGE, WTS_SESSION_LOCK/UNLOCK) to force Idle and re-arm the hook. || INSERTION LADDER (mirrors Mac AX-insert/clipboard ladder): 0. Resolve target at record-start AND re-resolve at delivery: GetForegroundWindow -> GetWindowThreadProcessId -> GetGUIThreadInfo(tid).hwndFocus + IUIAutomation::GetFocusedElement. 1. Guard rung: decline password fields (UIA_IsPasswordPropertyId 30019 == TRUE, or Win32 ES_PASSWORD), and decline+toast elevated targets (target integrity level > ours via OpenProcess+GetTokenInformation(TokenIntegrityLevel); SECURITY_MANDATORY_HIGH_RID 0x3000 vs MEDIUM 0x2000) - UIPI silently eats injection with no error. 2. Rung A - UIA ValuePattern.SetValue (clean, no clipboard) ONLY for known-good native single-line value controls (FrameworkId Win32/WinForm/WPF/XAML), because SetValue REPLACES the whole field and fires no per-keystroke event; verify by re-reading CurrentValue; NEVER for FrameworkId 'Chrome'/Electron (S_OK but no JS onChange) or terminals. 3. Rung B - Clipboard paste (PRIMARY, universal): snapshot prior clipboard (GetClipboardSequenceNumber + all formats) -> SetClipboardData CF_UNICODETEXT (13) + the privacy-exclusion formats ExcludeClipboardContentFromMonitorProcessing / CanIncludeInClipboardHistory=0 / CanUploadToCloudClipboard=0 (registered via RegisterClipboardFormat; keeps dictation out of Win+V history/Cloud Clipboard - the analog of macOS org.nspasteboard.ConcealedType) -> ~30-80ms settle -> SendInput Ctrl+V (VK_CONTROL 0x11 + 'V' 0x56), with Ctrl+Shift+V and Shift+Insert (VK_INSERT 0x2D) as ordered terminal fallbacks -> restore prior clipboard AFTER the paste is consumed via AddClipboardFormatListener/WM_CLIPBOARDUPDATE or a dynamic seqnum check (NOT a fixed huge sleep). Guard: SendInput does NOT reset key state - check GetAsyncKeyState for stuck Ctrl/Win from the still-held PTT chord and synthesize key-ups first. 4. Rung C - SendInput KEYEVENTF_UNICODE (0x0004) typing (wVk=0, wScan=UTF-16 unit; surrogate-aware, 2 INPUTs/BMP unit, batched into ONE SendInput; tag dwExtraInfo). Fallback when paste is blocked; avoid into legacy conhost pre-v1.16 (VK_PACKET mangles emoji) and active IME. 5. Rung D - decline + leave-on-clipboard + toast 'Dictation copied, press Ctrl+V' (secure/elevated/exclusive-fullscreen games). Per-app override table keyed by exe/AppUserModelID. Chunked paste (250/500/750/1000, default 250, split on whitespace) for terminal AI-CLI agents. || OVERLAY WINDOW FLAGS: WebviewWindowBuilder (Handy's exact non-macOS recipe): title 'Recording', resizable(false), inner_size ~256x46 tight to the pill, shadow(false) [THE critical flag - shadow-on is the #8308 'opaque box' bug], maximizable(false), minimizable(false), closable(false), accept_first_mouse(true) [macOS no-op, harmless], decorations(false), always_on_top(true), skip_taskbar(true), transparent(true) [MUST be set at creation, not toggled], focusable(false), focused(false), visible(false). tao maps focusable(false) -> WS_EX_NOACTIVATE (0x08000000, non-activating: target app keeps keyboard focus so injected text lands, buttons still get WM_LBUTTONDOWN), always_on_top -> WS_EX_TOPMOST (0x00000008), ignore_cursor_events -> WS_EX_TRANSPARENT (0x00000020) | WS_EX_LAYERED (0x00080000) [whole-window only; no per-region hit-test, Tauri #13070 - use Handy's tight-window design, NOT click-through polling]. Re-assert Z-order after show via force_overlay_topmost() SetWindowPos(HWND_TOPMOST, SWP_NOMOVE|SWP_NOSIZE|SWP_NOACTIVATE|SWP_SHOWWINDOW). Position bottom-center on the monitor WORK AREA (MONITORINFO.rcWork / SPI_GETWORKAREA), not the full rect, so it clears the taskbar; detect auto-hide via SHAppBarMessage ABM_GETSTATE ABS_AUTOHIDE (0x1) and add a ~48px bottom inset. DPI: rely on Tauri's default PerMonitorV2 manifest (DPI_AWARENESS_CONTEXT_PER_MONITOR_AWARE_V2); handle WM_DPICHANGED/WM_DISPLAYCHANGE and fractional scaleFactor. Optional SetWindowDisplayAffinity(WDA_EXCLUDEFROMCAPTURE) for privacy. Transparency mechanism: tao calls DwmEnableBlurBehindWindow with an empty region; WebView2 DefaultBackgroundColor=alpha 0 shows desktop through; semi-transparent pill via CSS rgba() over that (semi-transparent DEFAULT bg is unsupported - alpha must be 0 or 255). Set WEBVIEW2_DEFAULT_BACKGROUND_COLOR=00FFFFFF at startup if a first-frame white flash appears. Known multi-monitor gotcha (Handy #811): overlay can be invisible on secondary monitors - test. || TRAY: Tauri TrayIconBuilder (Win32 Shell_NotifyIcon underneath). Set menu_on_left_click(false) so left-click opens the Hub and right-click shows the context menu (Windows-native; Handy uses true). Call NIM_SETVERSION NOTIFYICON_VERSION_4 on every NIM_ADD. Menu mirrors Mac (Open WhimprFlow, Paste last transcript, Shortcuts, Microphone, Languages, Help, Settings). Use a FLAT Menu as root (Win11 crash bug #11363 on a Submenu root). Win11 hides new tray icons in the '^' overflow with no programmatic promotion - onboarding must tell users to drag it out, and core function must never depend on visibility (hotkey works regardless). || AUTOSTART: tauri-plugin-autostart -> HKCU Run key HKEY_CURRENT_USER\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Run (per-user, no admin; appears in Task Manager Startup). Default OFF, Settings toggle drives enable()/disable(). Self-heal: on launch, if the setting is ON but the Run value is missing, re-write it (plugins-workspace #771 'removed after one boot' bug). || MIC PERMISSION (biggest delta vs macOS TCC): unpackaged Win32 has NO per-app promptable consent. Two group toggles at Settings>Privacy>Microphone: packaged apps and 'Let desktop apps access your microphone' (governs ALL Win32 as one group). Enforced at the audio stack: when off, WASAPI/IAudioClient returns silence/E_ACCESSDENIED-class failure. Registry: HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\CapabilityAccessManager\\ConsentStore\\microphone\\NonPackaged\\Value = Allow/Deny. There is NO requestAccess API - detect blocked state (open capture -> silence/zeros, or read the registry Value==Deny) and deep-link to ms-settings:privacy-microphone via the opener plugin. Onboarding copy must branch per-OS. || INSTALLER + SIGNING + SMARTSCREEN/DEFENDER PLAN: NSIS -setup.exe with installMode: currentUser (=> %LOCALAPPDATA%, no UAC, matches Wispr's per-user model); ship NSIS-only for the auto-updater path (mixing MSI creates a second uninstaller and NSIS->MSI upgrade is not clean). Silent flag /S. Signing = Azure Trusted Signing (trusted-signing-cli), the same cheap cloud path Wispr uses (~72h short-lived leaf certs, chain Microsoft ID Verified CS AOC CA 04 -> ... 2020 root; RFC3161 timestamp for post-expiry trust; Authenticode SHA-256). SmartScreen: a fresh signing identity has zero reputation -> 'Windows protected your PC' until download volume accrues; mitigate by signing every release consistently, submitting the binary, and accumulating reputation. Defender: the sidecar's WH_KEYBOARD_LL hook + SendInput + clipboard is heuristically keylogger/RAT-shaped and is the #1 false-positive trigger - sign BOTH the app exe and the sidecar exe, do not pack/obfuscate, and submit samples to Microsoft (MSRC/WDSI) for analyst allowlisting before launch. Optional uiAccess='true' build (manifest, signed, installed under %ProgramFiles%) as an opt-in installer mode to drive elevated windows - but that requires per-machine install, breaking the per-user default. || AUTO-UPDATE: tauri-plugin-updater 2.10.0, one mechanism both OSes. Static latest.json (version, notes, pub_date, platforms.'windows-x86_64'.{url,signature}); ed25519/minisign signature (pubkey in tauri.conf.json plugins.updater.pubkey, TAURI_SIGNING_PRIVATE_KEY env; verification cannot be disabled). windows.installMode: passive (default). Flow check()->download()->install()->relaunch() (Windows app auto-exits on install). || ASR + LLM BACKEND TIERS BY HARDWARE (auto-detected via DXGI EnumAdapters1 VendorId 0x10DE NVIDIA/0x1002 AMD/0x8086 Intel + DedicatedVideoMemory, Vulkan deviceType, GlobalMemoryStatusEx RAM, __cpuidex leaf 7 AVX2 bit5/AVX512 bit16). Numbers are 7B Q4_0 tg128 scaled to 4B (~1.7x) unless noted. --- TIER W1 NVIDIA dGPU 6GB+ (RTX 3050/4050/4060+): ASR = ort Parakeet int8 (DirectML EP default low-friction, ~70-90% CUDA, ships DirectML.dll few-MB; CUDA opt-in +10-26%, ~373MB cudart download) ~150-450ms; LLM = llama.cpp Vulkan Qwen3-4B Q4_K_M ~74-108 tg (RTX 4060 Mobile Vulkan tg128 59.5 -> 4B ~101), committed ~0.9s, perceived ~0.2-0.3s streaming. MEETS Mac budget. --- TIER W2 AMD RDNA3+ APU (Radeon 780M/890M, Ryzen 7000+, >=16GB): ASR = ort Parakeet int8 DirectML or CPU ~0.5-1.5s; LLM = llama.cpp Vulkan 4B ~34-38 tg, committed ~1.9-2.1s, perceived ~0.35s streaming. OK with streaming; requires >=16GB (iGPU steals system RAM via UMA/VGM). --- TIER W3 Intel Iris Xe iGPU: ASR = ort Parakeet int8 CPU/XNNPACK ~1.5-2.5s; LLM = Vulkan 4B is too slow (~10-14 tg, ~5-7s) -> drop to Qwen3-1.7B (~22-30 tg, ~2.3-3.2s) or Llama-3.2-1B or default to Claude. --- TIER W4 CPU-only 8-core AVX2, 16GB: ASR = ort Parakeet int8 CPU ~5-11x realtime (~1.5-2.5s); LLM = 4B FAILS (~16-18 tg, ~4-4.5s) -> Qwen3-1.7B borderline (~35-40 tg, ~1.9s) or Llama-3.2-1B (~50-60 tg, ~1.5-1.8s) or Claude. --- TIER W5 floor 2-4 core AVX2, 4-8GB: ASR = ort Parakeet int8 CPU 2-4x (~3-6s) or whisper.cpp tiny/base q5; LLM = Llama-3.2-1B (~0.7GB) only, or ASR-only + Claude/raw. NEVER keep a 4B resident on 8GB (models+OS ~8.5GB before the browser -> paging). --- TIER W6 below AVX2 (Atom/old Celeron): no local LLM; whisper tiny/base or cloud only; local LLM gated on AVX2. Distribution: bundle ggml-cpu (all-variants, -DGGML_CPU_ALL_VARIANTS=ON -DGGML_BACKEND_DL=ON -DGGML_NATIVE=OFF so one binary runs AVX2 and non-AVX2) + ggml-vulkan.dll; download ggml-cuda.dll + cudart (~373MB) opportunistically for NVIDIA users. NOTE: like Handy, default ort to CPU (pyke's prebuilt ORT is AVX2-baseline and crashes pre-Haswell; DirectML/CUDA are opt-in power downloads with a non-baseline ORT build). All-Qwen ladder (1.7B->4B, Apache-2.0) preferred over Llama/Gemma for clean bundling. || MODEL DOWNLOAD UX: ship the installer WITHOUT weights (~tens of MB app + CPU/Vulkan DLLs); download the cleanup model on first local-mode use, PICKED BY DETECTED TIER (weak machines pull 1B ~0.7GB or 1.7B ~1.1GB, never the 4B ~2.4GB). Default dir %LOCALAPPDATA%\\WhimprFlow\\models (per-user, no admin, survives updates; user-relocatable). Resumable HTTP-range + SHA256 verify + progress UI + metered-connection warning + size cap requiring consent above it. Until present: default to Claude (if API key) or a 'download to enable local' prompt.

---

## Deltas to the existing Mac SPEC.md

Deltas are minimal and localized to the stack; the entire UI spec, Fn state machine semantics, cleanup rule table, prompts, and feature matrix are UNCHANGED in intent - only their implementation substrate moves from Swift to Rust/Tauri (+ Swift FFI shim for FluidAudio). || SPEC.md 'WHIMPRFLOW ARCHITECTURE' > 'CONFLICT RESOLVED - stack' and 'STACK': REPLACE 'FULLY NATIVE Swift (AppKit + SwiftUI via NSHostingView)' with 'Tauri v2 + Rust shared core + React/webview UI'. The claim 'No separate helper PROCESS required (one native process)' is REVERSED for the fidelity-critical subsystems: a SHARED-Rust native sidecar process now owns the hotkey hook + injection + secure-input + AX read (gap-track native-hook decision). Entitlements/Info.plist mostly survive (still non-sandboxed, hardened runtime, Developer-ID signed + notarized, outside MAS; add macOSPrivateApi:true in tauri.conf.json for transparency, which permanently disqualifies MAS - moot since AX+CGEventTap already force outside-MAS). Copyable-skeleton list changes: primary skeleton is now Handy (Rust/Tauri, overlay.rs, input.rs, llm_client.rs) + handy-keys 0.3.0; OpenSuperWhisper Swift is retained ONLY as the reference for the FluidAudio/CoreML Swift shim. || SPEC.md 'A. GLOBAL WINDOWING': the NSPanel recipe is now reached via tauri-nspanel PanelBuilder (PanelLevel::Status = level 25, borderless().nonactivating_panel(), can_join_all_spaces().full_screen_auxiliary().ignores_cycle(), no_activate, focusable false, transparent, shadow off) instead of a raw NSPanel subclass. Same primitives; add a per-OS geometry-offset seam. The ~440x300 transparent host + small pill + dynamic ignoresMouseEvents becomes Handy's tight-256x46-window design (no click-through polling) to avoid Tauri's lack of per-region hit-testing. || SPEC.md 'E. MENU-BAR (NSStatusItem)': becomes Tauri TrayIconBuilder (Shell_NotifyIcon on Win). activationPolicy(.accessory) stays on Mac; add the Windows tray behavior (menu_on_left_click(false), flat root menu). || SPEC.md 'TEXT PIPELINE': survives step-for-step but the runtime is Rust (cpal/rubato/vad-rs/rustfft) not AVAudioEngine; the frontmost-app + focused-AXUIElement + kAXSelectedTextRange capture now happens in the sidecar. || SPEC.md 'ASR ENGINE + FALLBACK': FluidAudio (Parakeet TDT 0.6B v2, ANE, 2.1% WER, 145.8x RTFx) is RETAINED as the Mac primary but now sits behind a shared AsrEngine trait and is called via a Swift FFI shim/sidecar from Rust; add transcribe-rs/ort CPU Parakeet as the Mac portable fallback (and Windows primary). WhisperKit tertiary is replaced by transcribe-cpp (whisper.cpp) with Metal on Mac / Vulkan on Win, keeping the large-v3-turbo fallback. Parakeet-EOU streaming preview stays optional/Mac-only (FluidAudio). || SPEC.md 'CLEANUP LLM RUNTIME + CLAUDE TOGGLE': 'embed llama.cpp in-process (link libllama)' becomes 'llama-cpp-2 0.1.151 in the Rust core, feature metal (mac) / vulkan (win)'. The optional MLX/mlx-swift toggle is DROPPED for v1. Model (Qwen3-4B-Instruct-2507 Q4_K_M), residency (--mlock), prompts, providers, latency targets, and the Claude Haiku 4.5 path (now reqwest, not URLSession) are unchanged. || SPEC.md 'TEXT-INSERTION STRATEGY': the ladder is unchanged in shape but implemented in the Rust sidecar - secure-input pre-check via Carbon IsSecureEventInputEnabled (macOS) stays; AX insert via objc2 AXUIElementSetAttributeValue(kAXSelectedTextAttribute); clipboard paste via enigo + hand-rolled NSPasteboard save/restore with changeCount + org.nspasteboard.ConcealedType; Cmd+V synth via CGEvent. Add the full Windows twin ladder (UIA/SendInput/clipboard) as a new parallel subsection. || SPEC.md 'PERMISSIONS FLOW': unchanged for Mac (Mic/Accessibility/Input-Monitoring, deep links, quit+relaunch for Input Monitoring) EXCEPT the process that must hold Accessibility + Input Monitoring is now the SIDECAR (an LSUIElement-style agent), not the main app - onboarding must grant the sidecar. Add the Windows mic-permission model (group toggle + registry + ms-settings deep link, no prompt). || SPEC.md 'STORAGE / DISTRIBUTION': Sparkle 2 EdDSA appcast becomes tauri-plugin-updater 2.10.0 (latest.json, minisign) shared across both OSes; UserDefaults becomes tauri-plugin-store; SQLite retention unchanged; SMAppService launch-at-login becomes tauri-plugin-autostart (SMAppService on Mac / HKCU Run on Win). || SPEC.md 'PHASED BUILD ORDER': superseded by the revised interleaved M0-M9 (below). || Feature matrix rows are unchanged in verdict; 'Global Fn hotkey (CGEventTap)' and 'Clipboard-paste insertion' now read 'in shared Rust sidecar', and each Mac-only integration row gains a Windows twin.

---

## Revised dual-platform build order

Sizing S~2-3d, M~1wk, L~2-3wk, XL~4-6wk. Dev on M4 Pro Mac first; Windows validated on real hardware/VM as shell lands. Build shared-first; land the Windows shell at M6 once the shared core + Mac path is proven. || M0 - Tauri shell + shared skeleton + permissions (M, both-OS scaffold). Tauri v2 workspace (whimpr-core/-tauri crates), .accessory/tray-only app, placeholder tray + hidden hub. Mac permissions onboarding (Mic/Accessibility/Input-Monitoring cards, deep links, polling auto-advance, quit+relaunch). Dev-signing for both; Developer-ID+notarize pipeline (Mac) and Azure Trusted Signing scaffold (Win, stub). Exit: fresh Mac install reaches all-granted; Tauri window renders on both OSes. || M1 - Native hotkey SIDECAR, Mac-first (XL, highest-risk primitive). Shared Rust sidecar bin: stdio JSON IPC contract; macOS CGEventTap via handy-keys/objc2_core_graphics on a dedicated CFRunLoop thread, consuming, Fn (0x3F/maskSecondaryFn) detect+suppress, 40ms debounce, 500ms cooldown; full state machine (HOLD, DOUBLE-TAP->LOCKED ~250-400ms, Esc-cancel, single-tap no-op); tap health watchdog (~5s, tap_is_enabled->reinstall), stale-key clear, IsSecureEventInputEnabled toast; shell<->sidecar heartbeat + respawn. Keybind validity UI. Exit: hold-Fn logs start/stop across apps incl. full-screen; no stuck-key bug; survives Terminal secure input; sidecar survives a simulated core-saturation stall. || M2 - Audio capture + local ASR behind the trait (M). cpal->rubato->vad-rs pipeline; AsrEngine trait; Mac FluidAudioEngine (Parakeet v2 batch, Swift FFI shim) + OrtParakeetEngine fallback; first-run model download UI + SHA256. Batch-finalize on release; basic pill waveform (rustfft->canvas). Exit: hold-Fn->speak->release->correct raw transcript <300ms on M4 Pro. || M3 - Text insertion ladder, Mac-first (M, in sidecar). secure-input pre-check -> AX insert (objc2) -> clipboard paste (save/restore + changeCount + ConcealedType) -> Unicode-type -> chunked paste for AI-CLI; failed-paste timer; per-bundleId override table. Exit: correct paste into TextEdit/Notes/Slack/Chrome/Terminal(chunked)/password(declines). || M4 - Cleanup LLM local + Auto Cleanup levels (L). llama-cpp-2 0.1.151 (metal), Qwen3-4B Q4_K_M, --mlock + prefill-on-hotkey-down + streaming + incremental insert; system prompt + few-shot + None/Light/Medium/High; deterministic gates -> raw fallback. Exit: end-to-end hold->clean->paste; over-editing rare at Light; perceived p50 ~300-500ms on M4 Pro. || M5 - Claude toggle (S). CleanupProvider trait + ClaudeCleanupProvider (reqwest, Haiku 4.5, thinking off, x-api-key), settings toggle + key field + healthCheck + auto-fallback. Exit: flip mid-session; 401->falls back. || M6 - WINDOWS SHELL LANDS (XL, the big interleave). Windows sidecar branch: WH_KEYBOARD_LL hook (windows 0.61.3), Ctrl+Win default + WindowsKeyUpSimulation, suppression, session-lock watchdog; Windows insertion ladder (UIA guard -> ValuePattern.SetValue -> clipboard+SendInput Ctrl+V/Shift+Insert with Win+V exclusion formats -> KEYEVENTF_UNICODE -> decline). Windows overlay branch (WebviewWindowBuilder shadow:false tight window + force_overlay_topmost, work-area/DPI math). Windows tray/autostart/mic-permission/ort-CPU-Parakeet/llama.cpp-Vulkan tiering + hardware detection + tiered model download. NSIS currentUser installer + Azure Trusted Signing + Defender/SmartScreen submission. tauri-plugin-updater latest.json for both. Exit: full hold->speak->paste loop works on real Win11 (Intel/NVIDIA/AMD); pill renders artifact-free at rest; Defender does not flag signed build. || M7 - Flow Bar polish + full state UI (M, shared). All states with 420ms morph, token geometry, error strings, 'Taking longer than usual', check/X, ping, hover-expand, tray + right-click Flow Menu, fixed dark theme. Per-OS geometry offsets. Exit: visual parity at cosmetic-delta values on both OSes. || M8 - Dictionary manual + auto-learn (L, shared + per-OS context read). Manual vocab + replacement rule; Double-Metaphone pre-filter (<=15 entries); auto-learn diff via AXObserver (Mac) / UIA focus+TextChanged (Win), wordfreq Zipf>=3.0 + caps/OOV gates + anti-poisoning caps. Exit: correcting Monvi->Manvi once makes it stick next time on both OSes. || M9 - Hub/history/settings/stats + CLONE-LATER (L each, parallelizable). Hub sidebar, encrypted SQLite history + retention + Undo-AI-edit, Settings (incl. Cleanup Engine + Auto Cleanup + per-OS panes), stats; then drag-to-dock+reflow+Esc-cancel, context-awareness, optional live-preview (Mac Parakeet-EOU), whisper.cpp fallback engine, mute-music-while-dictating, hands-free rolling-chunk long-session. || MAC-ONLY POLISH (interleaved, not a phase): FluidAudio ANE tuning, Parakeet-EOU streaming preview, MediaRemote mute, trackpad haptics (no-op on Win), macOS Fn/Globe 'Do Nothing' + built-in-Dictation conflict advice. || CROSS-CUTTING: paired eval harness (raw_asr, gold_cleaned) built at M4 to regression-test aggressiveness; coverage as the anti-over-editing metric; human eval not BLEU.

---

## Risk register

R1 HOOK TIMEOUTS / SILENT REMOVAL (Windows, HIGH). WH_KEYBOARD_LL callback exceeding LowLevelHooksTimeout (HKEY_CURRENT_USER\\Control Panel\\Desktop, default 5000ms, clamped to max 1000ms since Win10 1709) is SILENTLY removed with no notification (Microsoft: undetectable by design); handy-keys only re-hooks on suspend/session events. Co-resident ONNX+llama.cpp can starve the hook thread past 1000ms. MITIGATION: run the hook in the SEPARATE Rust sidecar process (cannot be starved by inference), keep the callback to a channel-send only (single-digit-us), raise the sidecar/hook-thread priority above inference, cap llama.cpp/ort thread pools to leave >=1 core, and shell-heartbeat the sidecar to respawn a hung helper. macOS CGEvent tap-timeout is recoverable (kCGEventTapDisabledByTimeout->CGEventTapEnable + 100ms health poll + full reinstall). || R2 DEFENDER / SMARTSCREEN FLAGS (Windows, HIGH). A global keyboard hook + SendInput + clipboard automation is keylogger/RAT-shaped; a fresh Azure Trusted Signing identity has zero SmartScreen reputation. MITIGATION: Authenticode-sign BOTH the app exe and the sidecar exe; no packing/obfuscation; submit samples to Microsoft (MSRC/WDSI) for analyst allowlisting pre-launch; accumulate download reputation; keep the sidecar's native surface minimal and auditable. || R3 iGPU / CPU LLM LATENCY (Windows, MEDIUM-HIGH). 4B Q4 on Intel Iris Xe (~10-14 tg, ~5-7s) and CPU-only 8-core (~16-18 tg, ~4-4.5s) BUSTS the perceived budget; 8GB machines page. MITIGATION: hardware-tiered model selection (drop to Qwen3-1.7B/Llama-3.2-1B on weak tiers, all-Qwen ladder), streaming to hide TTFT (~0.35-0.7s perceived), tiered download so weak machines never fetch the 4B, and Claude as the zero-download fallback; gate local LLM on AVX2 and >=16GB for the 4B. || R4 WEBVIEW RENDERING PARITY (both, MEDIUM). Tauri transparency 'opaque box' bug (#8308) is caused by shadow-on-by-default; black-EDGE artifacts (#14823/#14764) are resize/decoration/focus-bound. MITIGATION: Handy's exact recipe - transparent at CREATION, shadow(false), decorations(false), unparented, non-resizable tight 256x46 window; animate hover-expand via CSS inside a fixed window, not runtime resize; set WEBVIEW2_DEFAULT_BACKGROUND_COLOR=00FFFFFF if white-flash. FLIP-TO-ELECTRON gate: only if a static shadow-off pill goes black at rest on real Intel/NVIDIA/AMD Win11 (unverified, contradicted by the issue corpus). Also test Handy #811 (overlay invisible on secondary monitors). || R5 EXCLUSIVE-FULLSCREEN OVERLAY (both, MEDIUM, mostly unfixable). True DXGI exclusive-fullscreen (and macOS equivalent) bypasses the compositor; no WS_EX_TOPMOST / NSPanel level beats it (identical for Electron - a wash). MITIGATION: works over borderless/flip-model fullscreen (most modern games + Windows fullscreen-optimizations); re-assert HWND_TOPMOST on state transitions; document 'pill hidden during exclusive-fullscreen games; use borderless/windowed'; leave-on-clipboard fallback. || R6 ELEVATED WINDOWS / UIPI (Windows, MEDIUM). A Medium-IL app cannot hook or SendInject into a High-IL (Run-as-admin) foreground window; injection fails SILENTLY (neither GetLastError nor return value signals UIPI). MITIGATION: detect target integrity level (OpenProcess+GetTokenInformation, HIGH 0x3000 vs MEDIUM 0x2000) and decline+toast 'copied, press Ctrl+V; run WhimprFlow as admin to type here'; offer an optional uiAccess='true' signed %ProgramFiles% installer mode for parity (cost: per-machine install). macOS analog = secure-input (IsSecureEventInputEnabled) naming toast. || R7 ASR BUDGET ON MID-TIER MAC (macOS, MEDIUM). ort-CPU Parakeet misses ~300-500ms on base M-series/M1. MITIGATION: FluidAudio ANE primary behind the trait (the whole FORK-1 decision); ort-CPU only as portable fallback. || R8 CoreML EP DEAD-END (macOS, LOW but decision-load-bearing). Enabling ort-coreml gives NO ANE for Parakeet-TDT (LSTM + int8 QDQ unsupported) and can regress the decoder loop. MITIGATION: never rely on the CoreML EP for Parakeet; use native FluidAudio for ANE. || R9 tao/Tauri OVERLAY PATCHES (both, LOW). Handy pins tao to an unreleased rev via [patch]; Tauri's window layer occasionally needs source patches. MITIGATION: pin tao to Handy's known-good rev, budget for a [patch] entry, track Tauri overlay issues. || R10 DUAL-BINARY RELEASE DRIFT (both, LOW-MEDIUM). Sidecar + shell must version-match and keep IPC-schema compat across auto-update. MITIGATION: codegen the stdio JSON schema (quicktype-style), atomic bundle update via tauri-plugin-updater, IPC version handshake on spawn. || R11 SIDECAR IPC LATENCY (both, LOW). Every key transition + paste crosses a process boundary. MITIGATION: length-prefixed JSON over stdio is sub-ms to low-ms; keep paste ordering/clipboard timing in the sidecar (not round-tripped); it is non-critical for hold-to-talk. || R12 WIN-ON-ARM (Windows, LOW). Per-arch ONNX/whisper backend differs (Handy #1682: aarch64 GPU transcription fails). MITIGATION: ship CPU aarch64 ggml + arch-specific ort selection; treat Win-ARM GPU as best-effort.

---

## Open questions (need decisions or real-hardware tests)

- ELECTRON FLIP GATE (real hardware): build the static shadow:false + decorations:false + creation-time-transparent + unparented 256x46 pill and confirm it does NOT render a black/opaque box at rest on Intel iGPU, NVIDIA, and AMD Win11 GPUs. This is the single test that can overturn the Tauri decision.
- Sidecar vs in-process on macOS: confirm whether the shared-Rust sidecar is required on macOS or whether an in-process handy-keys CGEventTap (with the SPEC.md 5s health timer + full reinstall + stale-key watchdog) is stable enough under saturating FluidAudio+llama.cpp load - i.e. does every heavy transcription drop the Fn press during a stall?
- Windows hook liveness under inference saturation: measure whether the sidecar hook thread ever misses the 1000ms LowLevelHooksTimeout when llama.cpp Vulkan/CPU + ort peg all cores, and validate the heartbeat-respawn recovery path end-to-end.
- FluidAudio Swift<->Rust FFI shape: decide between an in-process Swift static-lib FFI shim vs a small Swift sidecar for the Mac ASR backend, and confirm it links cleanly under the Tauri build + Developer-ID notarization.
- Real Windows LLM latency: measure actual Qwen3-4B Q4_K_M decode (committed + streaming-perceived) on RTX 4050/4060 Vulkan, Radeon 780M/890M, Iris Xe, and an 8-core AVX2 CPU to confirm the tier table and the 1.7B/1B downgrade thresholds.
- DirectML vs CPU vs CUDA default for ort Parakeet on Windows: verify whether a non-AVX2-baseline ORT build with DirectML is shippable without the pre-Haswell crash Handy hit, or whether CPU must stay the default with DirectML/CUDA as opt-in downloads.
- Defender/SmartScreen pre-clearance: test whether the signed sidecar (WH_KEYBOARD_LL + SendInput) trips Defender heuristics on a clean Win11, and whether MSRC/WDSI sample submission + Azure Trusted Signing reputation clears it before public launch.
- Windows UIA insertion reliability + stalls: field-test ValuePattern.SetValue and TextPattern reads across Chromium/Electron, WinUI3, terminals, and Java/SWT, and confirm cross-process UIA reads run off-thread with a timeout to avoid the multi-second freeze (the VoiceInk #831 analog).
- tao overlay patch: confirm which tao rev / [patch] Handy uses for Windows overlay correctness on current Tauri, and whether WhimprFlow needs the same source-level pin.
- Ctrl+Win start-menu suppression robustness: verify across Win10/Win11 that dirtying-via-Ctrl plus swallowing the Win key-up reliably prevents the Start menu without breaking ordinary Win+E/Win+D when WhimprFlow is not triggering.
- Windows multi-monitor + fractional DPI overlay: test bottom-center anchoring, work-area/auto-hide-taskbar insets, and 125/150/175% scaling, and reproduce/mitigate Handy #811 (overlay invisible on secondary monitors).
- Model licensing/attribution for a shipped dual-platform binary: finalize Parakeet CC-BY-4.0 attribution (both OSes), FluidAudio Apache-2.0, Qwen3 Apache-2.0, and the download+checksum UX + licenses screen.
- Carry-over Mac open questions still live under the new stack: double-tap hands-free window (~250-400ms) user-tuning, Flow Bar bottom margin above Dock, pill fill hue authority, whether to ship live-preview streaming, MLX-vs-llama.cpp (now moot - MLX dropped), verifier trigger thresholds, AX auto-learn reliability, and chunked-paste sizing for current Claude Code/Cursor.

---

## Sources (213 URLs consulted)

- https://raw.githubusercontent.com/cjpais/Handy/main/src-tauri/Cargo.toml
- https://raw.githubusercontent.com/cjpais/Handy/main/src-tauri/tauri.conf.json
- https://github.com/cjpais/Handy/tree/main/src-tauri/src
- https://raw.githubusercontent.com/cjpais/Handy/main/src-tauri/src/overlay.rs
- https://raw.githubusercontent.com/cjpais/Handy/main/src-tauri/src/input.rs
- https://raw.githubusercontent.com/cjpais/Handy/main/src-tauri/src/shortcut/tauri_impl.rs
- https://github.com/cjpais/Handy/tree/main/src-tauri/src/shortcut
- https://raw.githubusercontent.com/cjpais/Handy/main/src-tauri/src/shortcut/mod.rs
- https://raw.githubusercontent.com/cjpais/Handy/main/AGENTS.md
- https://raw.githubusercontent.com/cjpais/Handy/main/src-tauri/Entitlements.plist
- https://github.com/ahkohd/tauri-nspanel
- https://raw.githubusercontent.com/ahkohd/tauri-nspanel/v2.1/README.md
- https://v2.tauri.app/learn/window-customization/
- https://v2.tauri.app/plugin/updater/
- https://v2.tauri.app/plugin/global-shortcut/
- https://github.com/tauri-apps/tauri/issues/13070
- https://github.com/tauri-apps/tauri/issues/11488
- https://github.com/tauri-apps/tauri/issues/5793
- https://github.com/tauri-apps/tauri/issues/7328
- https://github.com/tauri-apps/tauri/issues/9439
- https://www.electronjs.org/docs/latest/api/browser-window
- https://pub.dev/packages/window_manager
- https://raw.githubusercontent.com/leanflutter/window_manager/main/README.md
- https://raw.githubusercontent.com/cjpais/transcribe-rs/main/README.md
- https://github.com/utilityai/llama-cpp-rs
- https://docs.rs/crate/llama-cpp-2/latest/features
- https://github.com/NordSecurity/uniffi-bindgen-cs
- https://github.com/cjpais/Handy/issues/1682
- https://github.com/cjpais/Handy/issues/1669
- https://github.com/cjpais/Handy/issues/1706
- https://github.com/cjpais/Handy/issues/1661
- https://crates.io/crates/transcribe-rs
- https://crates.io/crates/tauri-plugin-macos-input-monitor
- https://dl.wisprflow.ai/windows/latest
- https://dl.wisprflow.com/wispr-flow/win32/x64/Wispr%20Flow%20Setup-v1.6.7.exe
- https://dl.wisprflow.com/wispr-flow/win32/x64/RELEASES
- https://dl.wisprflow.com/wispr-flow/win32/x64/WisprFlow-1.6.7-full.nupkg
- https://dl.wisprflow.com/wispr-flow/win32/x64/WisprFlow-1.6.30-full.nupkg
- https://wisprflow.ai/downloads
- https://wispr-packages.s3.us-west-2.amazonaws.com/wispr-flow/win32/x64/Wispr+Flow+Setup.exe
- Local extraction: WisprFlow-1.6.7-full.nupkg -> lib/net45/resources/app.asar (.webpack/main/index.js, renderer/hub/index.js, renderer/status/index.js), lib/net45/resources/Release/Wispr Flow Helper.exe, WisprFlow.nuspec
- Authenticode PKCS#7 parse of Wispr Flow Setup-v1.6.7.exe (openssl)
- /Users/mannbellani/WhimprFlow/docs/research/app-teardown.md (Mac v1.6.7 baseline for comparison)
- https://docs.wisprflow.ai/articles/2612050838-supported-unsupported-keyboard-hotkey-shortcuts
- https://docs.wisprflow.ai/articles/6391241694-use-flow-hands-free
- https://learn.microsoft.com/en-us/windows/win32/winmsg/lowlevelkeyboardproc
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/ns-winuser-kbdllhookstruct
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setwindowshookexa
- https://learn.microsoft.com/en-us/windows/win32/winmsg/about-hooks
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-registerhotkey
- https://learn.microsoft.com/en-us/windows/win32/inputdev/using-raw-input
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/ns-winuser-rawinputdevice
- https://docs.rs/global-hotkey/latest/global_hotkey/
- https://raw.githubusercontent.com/tauri-apps/global-hotkey/dev/src/platform_impl/windows/mod.rs
- https://raw.githubusercontent.com/cjpais/Handy/main/src-tauri/src/shortcut/handy_keys.rs
- https://github.com/Narsil/rdev
- https://docs.rs/rdev/
- https://en.wikipedia.org/wiki/Fn_key
- https://www.autohotkey.com/board/topic/51631-disable-windows-key-start-menu-but-not-shortcuts/
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-registerrawinputdevices
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/ns-winuser-keybdinput
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-sendinput
- https://learn.microsoft.com/en-us/windows/win32/dataxchg/clipboard-formats
- https://learn.microsoft.com/en-us/windows/win32/dataxchg/using-the-clipboard
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-getclipboardsequencenumber
- https://learn.microsoft.com/en-us/dotnet/framework/ui-automation/add-content-to-a-text-box-using-ui-automation
- https://learn.microsoft.com/en-us/windows/win32/winauto/uiauto-automation-element-propids
- https://learn.microsoft.com/en-us/windows/win32/api/uiautomationclient/nf-uiautomationclient-iuiautomation-getfocusedelement
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-getforegroundwindow
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-getguithreadinfo
- https://learn.microsoft.com/en-us/windows/win32/api/shobjidl_core/nf-shobjidl_core-iapplicationactivationmanager-activateapplication
- https://github.com/microsoft/terminal/issues/12977
- https://raw.githubusercontent.com/enigo-rs/enigo/master/src/win/win_impl.rs
- https://github.com/cjpais/Handy
- https://batchloaf.wordpress.com/2014/10/02/using-sendinput-to-type-unicode-characters/
- https://tdalon.blogspot.com/2021/04/ahk-paste-restore-clipboard-pitfall.html
- https://learn.microsoft.com/en-us/windows/win32/dataxchg/clipboard-formats (Cloud Clipboard and Clipboard History Formats section)
- local: /Users/mannbellani/WhimprFlow/docs/research/oss-clones.md (Handy input.rs teardown, VoiceInk #761 chunked paste, #758 RDP, #803 target-resolution)
- local: /Users/mannbellani/WhimprFlow/docs/research/macos-architecture.md (Mac ladder to mirror)
- https://k2-fsa.github.io/sherpa/onnx/pretrained_models/offline-transducer/nemo-transducer-models.html
- https://github.com/k2-fsa/sherpa-onnx/issues/2183
- https://huggingface.co/istupakov/parakeet-tdt-0.6b-v3-onnx
- https://huggingface.co/istupakov/parakeet-tdt-0.6b-v3-onnx/tree/main
- https://raw.githubusercontent.com/k2-fsa/sherpa-onnx/master/sherpa-onnx/csrc/provider.cc
- https://raw.githubusercontent.com/k2-fsa/sherpa-onnx/master/sherpa-onnx/csrc/session.cc
- https://k2-fsa.github.io/sherpa/onnx/pretrained_models/online-transducer/index.html
- https://docs.rs/sherpa-onnx
- https://crates.io/crates/sherpa-onnx
- https://github.com/thewh1teagle/sherpa-rs
- https://github.com/cjpais/transcribe-rs
- https://docs.rs/cpal/latest/cpal/
- https://onnxruntime.ai/docs/execution-providers/CoreML-ExecutionProvider.html
- https://onnxruntime.ai/docs/execution-providers/DirectML-ExecutionProvider.html
- https://onnxruntime.ai/docs/execution-providers/CUDA-ExecutionProvider.html
- https://ort.pyke.io/perf/execution-providers
- https://github.com/moonshine-ai/moonshine
- https://www.promptquorum.com/power-local-llm/local-whisper-stt-comparison-2026
- https://tesseraai.cloud/en/blog/whisper-large-v3-turbo-vs-large-v3-cpu-eu/
- https://weesperneonflow.ai/en/blog/2026-03-31-voxtral-whisper-open-source-speech-models-comparison-2026/
- https://arxiv.org/pdf/2604.14493
- https://deepwiki.com/k2-fsa/sherpa-onnx/7.1-gpu-support-(cuda-and-directml)
- https://github.com/k2-fsa/sherpa-onnx/releases
- https://huggingface.co/nvidia/parakeet-tdt-0.6b-v2
- https://github.com/FluidInference/FluidAudio
- https://snailtext.app/blog/how-whisper-cpp-works/
- https://github.com/ggml-org/llama.cpp/discussions/10879
- https://github.com/ggml-org/llama.cpp/discussions/15013
- https://knightli.com/en/2026/04/23/llama-cpp-gpu-benchmark-cuda-rocm-vulkan-scoreboard/
- https://medium.com/@techhara/llama-cpp-benchmark-cpu-vs-igpu-93b3cc40ece5
- https://www.myaihardware.com/llama-cpp-benchmarks
- https://zenvanriel.com/ai-engineer-blog/local-ai-integrated-graphics-vulkan-offload/
- https://github.com/ggml-org/llama.cpp/blob/master/docs/build.md
- https://github.com/ggml-org/llama.cpp/releases
- https://github.com/ollama/ollama/issues/15601
- https://onnxruntime.ai/blogs/accelerating-phi-3
- https://onnxruntime.ai/docs/genai/
- https://github.com/microsoft/onnxruntime-genai
- https://learn.microsoft.com/en-us/windows/ai/apis/phi-silica
- https://learn.microsoft.com/en-us/windows/ai/overview
- https://learn.microsoft.com/en-us/windows/ai/apis/
- https://learn.microsoft.com/en-us/windows/ai/npu-devices/
- https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/what-is-foundry-local
- https://www.foundrylocal.ai/models
- https://huggingface.co/meta-llama/Llama-3.2-1B-Instruct
- https://huggingface.co/google/gemma-3-1b-it
- https://huggingface.co/Qwen/Qwen3-1.7B
- https://blogs.windows.com/windowsexperience/2024/12/06/phi-silica-small-but-mighty-on-device-slm/
- https://learn.microsoft.com/en-us/windows/win32/winmsg/extended-window-styles
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setprocessdpiawarenesscontext
- https://learn.microsoft.com/en-us/windows/win32/hidpi/dpi-awareness-context
- https://learn.microsoft.com/en-us/windows/win32/hidpi/setting-the-default-dpi-awareness-for-a-process
- https://learn.microsoft.com/en-us/windows/win32/shell/abm-getautohidebar
- https://learn.microsoft.com/en-us/windows/win32/shell/appids
- https://learn.microsoft.com/en-us/windows/win32/api/shellapi/nf-shellapi-shell_notifyiconw
- https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation
- https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/code-signing-options
- https://support.microsoft.com/en-us/windows/privacy/turn-on-app-permissions-for-your-microphone-in-windows
- https://v2.tauri.app/reference/config/
- https://v2.tauri.app/reference/javascript/api/namespacewindow/
- https://v2.tauri.app/distribute/windows-installer/
- https://v2.tauri.app/distribute/sign/windows/
- https://v2.tauri.app/plugin/autostart/
- https://v2.tauri.app/learn/system-tray/
- https://v2.tauri.app/plugin/notification/
- https://github.com/tauri-apps/tauri/issues/8308
- https://github.com/tauri-apps/tauri/issues/11363
- https://github.com/tauri-apps/tauri/issues/7719
- https://github.com/tauri-apps/tauri/issues/6652
- https://github.com/tauri-apps/plugins-workspace/issues/771
- https://github.com/tauri-apps/window-vibrancy
- https://lib.rs/crates/window-vibrancy
- https://crates.io/crates/tauri-winrt-notification
- https://raw.githubusercontent.com/cjpais/Handy/main/src-tauri/src/lib.rs
- https://www.electronjs.org/docs/latest/tutorial/custom-window-interactions
- https://github.com/electron/electron/issues/11830
- https://github.com/microsoft/PowerToys/issues/27088
- https://backgrind.com/blog/borderless-vs-exclusive-fullscreen/
- https://weblog.west-wind.com/posts/2025/Jul/20/Fighting-through-Setting-up-Microsoft-Trusted-Signing
- https://knowledge.digicert.com/alerts/ev-signed-application-showing-microsoft-defender-smartscreen-warnings
- https://www.microsoft.com/en-us/wdsi/filesubmission
- https://windowsforum.com/threads/windows-privacy-for-desktop-apps-what-win32-access-means-for-your-data.399339/
- https://sysmansquad.com/2023/01/21/microphone_app_permissions/
- https://learn.microsoft.com/en-us/windows/apps/develop/ui/system-backdrops
- https://raw.githubusercontent.com/cjpais/transcribe-rs/main/Cargo.toml
- https://raw.githubusercontent.com/cjpais/transcribe-rs/main/src/accel.rs
- https://raw.githubusercontent.com/cjpais/transcribe-rs/main/src/onnx/session.rs
- https://raw.githubusercontent.com/cjpais/transcribe-rs/main/src/onnx/parakeet/mod.rs
- https://api.github.com/repos/cjpais/transcribe-rs/git/trees/main?recursive=1
- https://raw.githubusercontent.com/FluidInference/FluidAudio/main/Documentation/Benchmarks.md
- https://raw.githubusercontent.com/FluidInference/FluidAudio/main/README.md
- https://github.com/istupakov/onnx-asr
- https://mikeesto.com/posts/parakeet-tdt-06b-v2/
- https://github.com/anvanvan/mac-whisper-speedtest
- /Users/mannbellani/WhimprFlow/docs/research/local-asr.md
- /Users/mannbellani/WhimprFlow/docs/research/oss-clones.md
- /Users/mannbellani/WhimprFlow/docs/research/macos-architecture.md
- https://github.com/tauri-apps/tauri/issues/8308 (V2 window.transparent not work; maintainer FabianLars: shadows-on-by-default cause the transparency issue; fix shadow:false; ahaoboy confirms decorations:false+transparent:true+shadow:false works; env Win10 22635, WebView2 119.0.2151.72, Tauri 2.0.0-alpha.17, wry 0.34.2, tao 0.23.0; state: open)
- https://api.github.com/repos/tauri-apps/tauri/issues/8308/comments (comment thread confirming shadow:false workaround)
- https://github.com/tauri-apps/tauri/issues/7328 (actually: taskbar occluded with transparent+fullscreen on Tauri v1.4.1; closed as not planned; NOT a v2 above-fullscreen overlay artifact)
- https://github.com/tauri-apps/tauri/issues/13070 (feat: Transparent Window Support Click-Through; confirms NO per-region hit-testing; workaround = multiple WebViews ~20-30MB each; closed as duplicate)
- https://github.com/tauri-apps/tauri/issues/14823 (windows10 strange black border for transparent/non-shadow/non-decoration window; occurs on RESIZE; shadow:false already set; open; Tauri 2.9.5, wry 0.53.5, WebView2 144, Win10 19045)
- https://github.com/tauri-apps/tauri/issues/14764 (Windows ghost/black titlebar during drag or focus change on transparent windows; requires titlebar+focus change; status: upstream/WebView2; Tauri 2.9.5, WebView2 144)
- https://github.com/tauri-apps/tauri/issues/13176 (black border with decorations:false + transparent:true; reporter had shadow:true; Tauri 2.4.0, Win10 22H2)
- https://github.com/tauri-apps/tauri/issues/12450 (transparent CHILD window renders black; workaround: remove parent relationship; Tauri 2.2.3, WebView2 131, Win10)
- https://api.github.com/search/issues?q=repo:tauri-apps/tauri+transparent... (enumerated issue list incl. #1564 white-flash, #15718 CEF/Linux black screen)
- https://learn.microsoft.com/en-us/dotnet/api/microsoft.web.webview2.core.corewebview2controller.defaultbackgroundcolor (DefaultBackgroundColor supports only alpha 0 or 255 - translucent NOT supported; transparent shows hosting app content on OS>Win7; no transparency on Win7; WEBVIEW2_DEFAULT_BACKGROUND_COLOR env var fixes white flicker; property since WebView2 SDK 1.0.1010-prerelease/1.0.1020.30)
- https://raw.githubusercontent.com/cjpais/Handy/main/src-tauri/tauri.conf.json (app.windows = []; macOSPrivateApi true; productName Handy v0.9.3; overlay created programmatically)
- https://raw.githubusercontent.com/cjpais/Handy/main/src-tauri/src/overlay.rs (overlay builder flags; OVERLAY_WIDTH=256.0, OVERLAY_HEIGHT=46.0; force_overlay_topmost SetWindowPos HWND_TOPMOST SWP_NOMOVE|SWP_NOSIZE|SWP_NOACTIVATE|SWP_SHOWWINDOW once per state transition, no timer; calculate_overlay_position bottom-center; no ignore_cursor_events; EMIT_THROTTLE_MS=33; macOS uses NSPanel PanelLevel::Status nonactivating)
- https://raw.githubusercontent.com/cjpais/Handy/main/src-tauri/src/lib.rs (main window 680x570 hidden; utils::create_recording_overlay)
- https://raw.githubusercontent.com/cjpais/Handy/main/src/overlay/RecordingOverlay.tsx (no setIgnoreCursorEvents, no getCurrentWindow, no pointer-events CSS; cancel button onClick commands.cancelOperation)
- https://api.github.com/search/issues?q=repo:cjpais/Handy... (Windows overlay issues #508 state, #811 invisible on secondary monitors, #1418 CPU after stop, #1570 crash on close, #1683 update dialog invisibility; NONE about transparency/black-box)
- https://raw.githubusercontent.com/tauri-apps/tao/dev/src/platform_impl/windows/window.rs (transparent via DwmEnableBlurBehindWindow empty region CreateRectRgn(0,0,-1,-1) DWM_BB_ENABLE|DWM_BB_BLURREGION; skip_taskbar via ITaskbarList::DeleteTab; focusable/always_on_top via WindowFlags; no accept_first_mouse on Windows)
- https://raw.githubusercontent.com/tauri-apps/tao/dev/src/platform_impl/windows/window_state.rs (to_window_styles: if !FOCUSABLE style_ex|=WS_EX_NOACTIVATE; if ALWAYS_ON_TOP style_ex|=WS_EX_TOPMOST; if IGNORE_CURSOR_EVENT style_ex|=WS_EX_TRANSPARENT|WS_EX_LAYERED)
- https://www.electronjs.org/docs/latest/api/browser-window (setIgnoreMouseEvents(ignore,{forward}) forward is macOS+Windows; alwaysOnTop levels incl screen-saver; transparent option; focusable/skipTaskbar)
- https://www.electronjs.org/docs/latest/tutorial/custom-window-styles (transparent window limitations verbatim: cannot click through transparent area see #1335; not resizable; CSS blur only web contents; not transparent when DevTools open; Windows cannot maximize via system menu/double-click titlebar; macOS no native shadow)
- https://v2.tauri.app/learn/window-customization/ (transparency set at window creation time; macOS uses TitleBarStyle::Transparent + objc2-app-kit)
- /Users/mannbellani/WhimprFlow/docs/research/ui-flow-bar.md (Wispr real overlay ~440x300 transparent, pill ~70px bottom-anchored, ~400ms self-reposition loop - a WISPR behavior not Handy's)
- /Users/mannbellani/WhimprFlow/docs/research/oss-clones.md (Handy = Rust+Tauri 2.x MIT, ships the Windows overlay; VoiceInk/OpenSuperWhisper native macOS NSPanel recipes for the macOS side)
- https://raw.githubusercontent.com/cjpais/Handy/main/src-tauri/src/shortcut/handler.rs
- https://github.com/handy-computer/handy-keys
- https://docs.rs/handy-keys/latest/handy_keys/
- https://lib.rs/crates/handy-keys
- https://github.com/handy-computer/handy-keys/tree/main/src
- https://github.com/handy-computer/handy-keys/tree/main/src/platform
- https://github.com/handy-computer/handy-keys/tree/main/src/platform/windows
- https://github.com/handy-computer/handy-keys/tree/main/src/platform/macos
- https://raw.githubusercontent.com/handy-computer/handy-keys/main/src/platform/windows/listener.rs
- https://raw.githubusercontent.com/handy-computer/handy-keys/main/src/platform/macos/listener.rs
- https://raw.githubusercontent.com/handy-computer/handy-keys/main/README.md
- https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setwindowshookexw
- https://raw.githubusercontent.com/Narsil/rdev/main/README.md
- https://raw.githubusercontent.com/phracker/MacOSX-SDKs/master/MacOSX10.15.sdk/System/Library/Frameworks/CoreGraphics.framework/Versions/A/Headers/CGEventTypes.h
- /Users/mannbellani/WhimprFlow/docs/research/app-teardown.md